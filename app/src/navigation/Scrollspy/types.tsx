import React from 'react';

type ScrollspyProps = {
  className?: string;
  offset?: number;
  onElement?: boolean;
  tag?: React.ComponentProps<any>;
  [rest: string]: any;
};

export { ScrollspyProps };
