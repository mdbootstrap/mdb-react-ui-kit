import React from 'react';

type TableHeadProps = {
  className?: string;
  dark?: boolean;
  light?: boolean;
  tag?: React.ComponentProps<any>;
  [rest: string]: any;
};

export { TableHeadProps };
